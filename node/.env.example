# ==============================================
# PRICE CHECKER CONFIGURATION
# ==============================================

# Run mode: 'full' (API + workers), 'api' (API only), 'worker' (workers only)
# - Price Server: MODE=api
# - Your PC: MODE=worker
MODE=full

# PostgreSQL database (stores products, prices, job queue, etc.)
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=cyber
PG_USER=
PG_PASSWORD=

# API server
API_PORT=3003
API_SECRET=your-api-secret-key

# Browser workers
WORKER_COUNT=3
HEADLESS=true

# Scheduling (in seconds)
CHECK_INTERVAL=21600          # 6 hours
FRESHNESS_THRESHOLD=1800      # 30 minutes
PRODUCT_SYNC_INTERVAL=3600000 # 1 hour (in ms)

# Request delays (in ms)
DELAY_BETWEEN_REQUESTS=2000

# Source filtering - 'local', 'international', or 'all'
SOURCES=all

# ==============================================
# LOCAL WORKER CONFIGURATION (Your PC)
# ==============================================
# When running workers on your local PC to bypass bot protection:
# - Copy this file to .env
# - Set PG_HOST to your Price Server's IP/hostname
# - Set HEADLESS=false for debugging
# - Increase WORKER_COUNT for more parallel checks
